# Dataset

This directory will contain the raw and processed datasets used for the DVA_Project. Suggested subfolders:
- raw/ : original unmodified data
- processed/ : cleaned and preprocessed data ready for analysis or model training

Do NOT store large binary files in the repository; use Git LFS or external storage if needed.
